# Application configuration for test profile
app.name=SocialSports (Test)
app.version=0.1.0

# AWS DynamoDB configuration
aws.dynamodb.endpoint=http://localhost:8000
aws.dynamodb.region=eu-west-1

# WhatsApp configuration (dummy values for testing)
whatsapp.business.phone.number.id=dummy
whatsapp.api.version=v17.0
whatsapp.access.token=dummy

# Event configuration
event.minimum.advance.hours=0
event.auto.cancel.hours=2
event.free.usage.limit=999

# AWS credentials for local development
aws.credentials.accessKey=dummy
aws.credentials.secretKey=dummy

spring.main.allow-bean-definition-overriding=true

# OpenAI Configuration (dummy key for testing)
openai.api.key=dummy-key
openai.api.model=gpt-4
openai.api.temperature=0.7
openai.api.max-tokens=150

# JWT Configuration
jwt.secret=socialSportsSecretKey12345678901234567890
jwt.expiration=86400

# Test configuration
test.data.load=true 